p : (+) { q ! 1.0 : ok . (+) { r ! 1.0 : notify . end } }
  + (+) { q ! 1.0 : go . mu t . (+) { q ! 1.0 : go . t } }

q : & {
        p ? ok . end,
        p ? go . mu t . & { p ? go . t }
      }

r : & { p ? notify . end }
